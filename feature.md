# anm 功能设计文档

> 本文档描述 anm（anotemanager）的功能设计与架构现状，与根目录 `readme.md`（需求文档）保持同步；**两者冲突时以 readme.md 为准**。
> 状态：核心功能已实现；凡标注「待定」「未来」处均未定稿或未排期。

---

## 0. 定位

anm 是一个**个人笔记系统的管理器**（纯个人定制项目）：以常驻服务 **anm-core** 为核心（一核心三应用），把作者既有的「文件夹 + 文本文件」笔记库（znote）升级为人机共享的持久记忆层——供人操作的 CLI（anm）、快速写入（anw）、Windows 薄壳（anm-tray-win）三个应用经 IPC 访问服务，MCP Server 内置在服务中供 AI agent 操作。

- 不做市场化、多用户、权限体系、企业级审计等通用化设计；一切复杂度取舍以「对作者一个人是否好用」为唯一标准。
- 不重组作者已有的笔记目录结构；目录组织权始终在作者手里。
- 设计原则与完整规格见 [readme.md](readme.md)：消费回路（human fetch / agent fetch / exhibit）、两类场景（决策 / 自由思维）、AI 写入自主权原则（§6）、标签系统规格（§11）、分层架构（§9）、一核心三应用（§13）、场景剧本层（§14）、开放问题（§15）。

---

## 1. 功能总览

> 阶段说明：**核心**＝已实现；**待定**＝方向已认可、细节未定；**未来**＝未排期或 readme 未细化的旧规划。

| 类别 | 功能 | 一句话描述 | 阶段 |
|------|------|-----------|------|
| 标签系统 | 标签行识别 | 识别整行仅含一个或多个 `@xxx` 的行，每个 `@xxx` 视为一个标签；作用域为整篇笔记 | 核心 |
| 标签系统 | 段落标签 | 段落开头（行首）出现一个或多个 `@xxx` 且后随任意全/半角空格或缩进 → 识别为该段落的段落标签，仅可出现在段落开头；原整行纯标签改称文本标签；作用域/检索/与 AI 自主权边界联动仍待细化 | 核心（识别与提取已实现） |
| 标签系统 | 标签行置顶整理 | 将文档中已识别的标签行移动到文档开头（纯位置整理：不合并、不排序、不改写内容）——自主状态下对已有标签唯一允许的操作 | 核心 |
| 标签系统 | 新增标签 | 在文档开头标签区追加不存在的标签行，不改动已有标签行 | 核心 |
| 标签系统 | 标签查询 | 按一个或多个标签筛选笔记（任一命中） | 核心 |
| 标签系统 | 标签图（共现关系） | 标签共现关系网络 + 独立于 AI 对话的可视化面板（可手动修剪：删除/合并标签） | 待定 |
| 标签系统 | 命名空间标签 | `@for:comfyui` 语法兼容，语义与检索设计明确搁置（不在 v1 范围） | 待定 |
| 标签系统 | 标签补全 / 重命名 / 统计 / 悬空检测 | 旧规划，readme 未细化，未排期 | 未来 |
| 查询浏览 | 目录浏览 | 一级目录枚举（CLI、shell 补全、MCP 共用） | 核心 |
| 查询浏览 | 标题查询 | 按文件名 / 标题关键字检索（子串、大小写不敏感） | 核心 |
| 查询浏览 | 全文检索 | 正文关键词搜索，返回命中片段与次数（core / MCP 已实现，CLI 未暴露） | 核心 |
| 查询浏览 | 最近笔记 | 按修改时间列出最近更新的笔记（MCP） | 核心 |
| 查询浏览 | 时间范围查询 | readme §12 列出的查询原语之一 | 待定 |
| 查询浏览 | 组合筛选 / 聚合原语 | 多来源组合查询（如 working 现状 + problem 列表 + 待分诊条目），只拼数据不组织语言 | 待定 |
| 查询浏览 | 结构化输出 | 支持 `--json` 机器可读输出，供脚本与 MCP 复用 | 核心 |
| CLI 交互 | 子命令体系 | 统一的 `anm <subcommand>` 命令结构 | 核心 |
| CLI 交互 | shell 补全 | bash / zsh / fish tab 补全一级目录、子命令与标签 | 核心 |
| CLI 交互 | anw 快速写入 | 独立命令将参数追加写入默认 skatch.md（inbox 入口） | 核心 |
| CLI 交互 | 面板 / TUI | 层级目录浏览、编辑器打开；决策场景开场信息（working 现状、problem 列表、待分诊条目）在完全不接 AI 时可用；标签图可视化面板 | 待定 |
| CLI 交互 | 界面自定义 / AI 自定义 skill | 旧规划保留 | 未来 |
| MCP | MCP Server | 官方 rmcp 实现，**内置于 anm-core 服务**：HTTP 常驻端点（默认本地 HTTP）+ stdio 会话（`anm-core --stdio` 供客户端 spawn） | 核心 |
| MCP | 工具（tool） | 按需调用的原语：按标签 / 目录 / 内容检索、读笔记、新建、写 inbox、新增标签、标签行置顶、打开编辑器（13 个） | 核心 |
| MCP | 资源（resource） | 常驻上下文、默认只读：**`anm://facts` 通用事实已实现**（← 笔记库 `.agentspace/fact.md`，人工维护、永远以当前文件为准）；标签索引摘要、当前项目概览摘要待定 | 核心（facts）/ 待定（摘要类） |
| MCP | 记忆总线 | 作为 agent 跨会话的第一内存取指-加载通道 | 核心 |
| 数据索引 | 配置管理 | `~/.anm/config.toml`：笔记系统注册、编辑器、skatch 路径、`[mcp]` 传输段、`[server]` IPC 段 | 核心 |
| 数据索引 | 现场扫描 | 查询默认现场扫描（类似 ripgrep），不预建持久索引；持久索引仅在未来实测成为瓶颈时按需引入 | 核心 |
| anm-core 服务 | 后台常驻 | 唯一长期后台运行的服务进程（文件监听 + IPC + MCP 一体） | 核心 |
| anm-core 服务 | 文件监听 | 监听笔记目录变化，实时感知外部修改（非唯一入口：只观察，不拦截） | 核心 |
| anm-core 服务 | IPC 端点 | TCP + JSON 行协议（`Envelope` = 可选令牌 + 请求），供 anm / anw / anm-tray-win 查询与低风险写入；`[server] token` 配置后校验令牌；协议类型服务端与客户端共用 | 核心 |
| anm-core 服务 | 笔记内容读写（IPC） | `ReadNote` / `WriteNote`：托盘内置编辑器经 IPC 读写笔记全文（**人机通道**，MCP 不暴露 `WriteNote`；路径白名单校验）——跨机器也能编辑服务端笔记 | 核心 |
| anm-core 服务 | 定时 / 条件提醒 | 执行作者显式设定的强时间/条件绑定提醒（如「晚 8 点提醒我打电话」） | 待定 |
| Windows | 薄壳原则 | Windows 侧仅为薄壳，主要功能全部运行于 Linux / WSL | 已规划 |
| Windows | 托盘 / 快捷键 / 交互窗口 / TCP | anm-tray-win：单例常驻托盘 + 全局快捷键（默认 Alt+Shift+Z，菜单可重设；**再按一次取消激活**）呼出覆盖层（半透明变暗背景 + 纯色卡片环绕 + 居中输入框，输入框为 anw 语义：回车写入 skatch.md；点击打开 / 拖动记忆位置 / 点击空白取消；**右上角连接状态栏**），经 TCP 连接 anm-core 服务（托盘菜单可配置服务地址与访问令牌并持久化） | 核心（已实现，作者验收通过） |
| Windows | 卡片美化与滚动 | 细滚动条（起点在标题线之下）+ 滚轮滚动、贯通分隔线、行悬停贯通阴影条、标题栏收窄居中、卡片右上角「+」新建按钮（固定常驻）、滚动位置跨激活记忆（子目录行 ▸、临时子卡片与主卡片同配色）；**GUI 框架迁移仍为后续选项**：候选 egui/eframe（纯 Rust、透明窗口、GNU 可交叉编译），备选 iced，Tauri 排除——验证通过后可将外壳渲染迁到框架，核心不变 | 核心（已实现；框架迁移可选） |
| Windows | 内置临时编辑器 | 点击文本类条目 → 中央输入框切换为内置文本编辑框（多行、滚动、Ctrl+S 保存、Esc 退出自动保存、**文件名可改**（同目录重命名）、「打开所在位置」按钮开资源管理器到所在目录；上/下贯通分割线分隔头部/编辑区/功能区）；**内容读写经 IPC 由 anm-core 完成（跨机器可编辑）**；非文本文件退化系统默认打开（打不开时轻提示，不弹系统错误框） | 核心（已实现） |
| Windows | 轻提示（toast） | 右上角小窗自动隐藏：设置生效 / 快捷键更新 / 打开失败等反馈 | 核心（已实现） |
| Windows | 斜杠命令 | 中央输入框支持 `/命令`，与 anm CLI 命令一致：`/help` `/inbox|/anw` `/find` `/search` `/tags` `/ls` `/open` | 核心（已实现） |
| Windows | 临时子卡片 | 卡片显示子文件夹（▸ 行）；点击子文件夹在当前卡片位置生成临时子卡片（紫调边框区分）；取消激活后不保留（不写入 layout.json） | 核心（已实现） |
| 生命周期 | 新建笔记 | 在指定目录创建新笔记（白名单校验、绝不覆盖） | 核心 |
| 生命周期 | 追加内容 | 向笔记追加内容（自主状态下允许的"低风险追加"类写入原语） | 待定 |
| 生命周期 | 重命名 / 移动 / 归档 | 移动笔记归档到位属于自主允许的操作；其余仅作者显式指令下执行 | 待定 |
| 生命周期 | 删除 | 自主状态下一律不允许，仅作者显式指令下执行 | 待定 |
| 生命周期 | inbox 分流（分诊） | 把 skatch.md 条目分流至合适板块；触发方式未定（readme §15 开放问题 5） | 待定 |
| 生命周期 | 笔记模板 | 新建笔记时套用预置模板 | 未来 |
| 统计洞察 | 使用统计 / 认知洞察 / 操作日志 / 导出 | 旧规划保留，未排期 | 未来 |

---

## 2. 架构

分层（readme §9，一核心三应用）：

```
Layer 0  存储实体层     纯文本 + 行内标签，文件系统本身即唯一真相源
Layer 1  服务层         anm-core（常驻）：现场扫描查询 / 文件监听 / 内置 MCP（HTTP + stdio）/ IPC / （规划）定时提醒
Layer 2  应用层         anm（人：CLI/面板）· anw（快速写入）· anm-tray-win（Windows 薄壳）——全部经 IPC 访问服务
Layer 3  场景编排层     不写死在代码里，是给 agent 的"剧本"文档（延伸自 AGENTS.md）
```

crate 与职责：

```
anm-core   (lib + bin)  — 常驻服务：lib = 确定性逻辑（config / tags / query / path / notes /
                          inbox / tree / protocol）；bin = 服务外壳（文件监听 + IPC + 内置 MCP）
anm-cli    (lib + bin)  — 应用：anm（CLI/面板，经 IPC 访问服务）+ anw（快速写入）；本地动作
                          仅 init / open / completion；client 模块为共用 IPC 客户端
anm-tray-core (lib)    — 托盘共享核心（跨平台纯逻辑）：卡片布局/命中/拖动/滚动、状态模型、命令（anw+斜杠）、IPC 客户端
anm-tray-win (bin)      — Windows 托盘外壳：窗口/渲染/托盘/热键/编辑器 UI，逻辑全在 anm-tray-core
（规划）anm-tray-wayland / anm-tray-android — 只写平台外壳，复用 anm-tray-core
```

原则：

- **一核心三应用**：anm-core 是唯一长期后台运行的服务进程；anm / anw / anm-tray-win 均为经 IPC 访问它的薄壳客户端；MCP 是 anm-core 的内置功能，不单独成应用（stdio 模式下客户端直接 spawn `anm-core --stdio`）。
- **所有业务逻辑在 anm-core 的 lib 中**，服务外壳与三个应用只做接口适配。
- anm-core **不是唯一入口**：用户可随时用任何形式修改笔记目录，服务只负责观察（与未来的提醒）。
- **查询默认现场扫描，不预建持久索引**（readme §10）；任何上层数据结构都不得成为事实的第二来源。
- **AI 写入自主权原则**（readme §6）：自主状态只允许"新增/低风险追加"（写新笔记、追加内容、新增标签、移动笔记归档到位、标签行置顶）；修改/删除/覆盖已有内容一律不允许，仅作者显式指令下可执行任意操作。边界靠"不提供自主调用原语"保证，不设计 ACL/policy 引擎；IPC 的 `WriteNote` 是**人机通道**（托盘编辑器），MCP 侧不暴露。
- **场景剧本在文档层**（readme §14）：决策场景 / 自由思维场景的行为规范以类似 AGENTS.md 的文档/skill 形式维护，不写死在 MCP 工具实现里，anm-core 不感知场景概念。

---

## 3. 构建与交叉编译（代码规范）

### Linux（WSL）构建

```bash
cargo build --workspace        # Linux 上构建全部三个 crate（anm-tray-win 得到占位二进制）
cargo test --workspace         # 单元测试（含卡片布局 / WSL 路径转换等跨平台单测）
```

### Windows 交叉编译（GNU 路线，已选定）

从 WSL 交叉编译 Windows 可执行文件（rustup 工具链 + MinGW 链接器）：

```bash
rustup target add x86_64-pc-windows-gnu            # 一次性：加 Windows 编译目标
sudo apt install gcc-mingw-w64-x86-64              # 一次性：MinGW 链接器
cargo build --target x86_64-pc-windows-gnu --release -p anm-tray-win
# 产物: target/x86_64-pc-windows-gnu/release/anm-tray-win.exe → 拷到 Windows 运行
```

- 依赖全部为纯 Rust（rmcp / axum / tokio / notify / windows-sys），GNU 路线即可，不需要 MSVC。
- 交叉编译出的 `.exe` 在 WSL 里不能运行（PE 格式），用 `file <exe>` 验证产物。
- MSVC 目标（`x86_64-pc-windows-msvc` + cargo-xwin/zigbuild）仅在需要 MSVC ABI 兼容时备用，当前不采用。

### Windows 侧运行前提（anm-tray-win）

- anm-core 服务默认绑定 `127.0.0.1:17370`（本机 / WSL2 localhost 转发）；**跨机运行**（如 anm-core 在 zmain、托盘在 101）需把 `[server]` 的 host 改为 `0.0.0.0`，并在托盘「设置服务地址…」中填入 `主机:端口`（持久化到 `%APPDATA%/anm-tray-win/config.json`；环境变量 `ANM_SERVER_ADDR` 仍可覆盖）；
- 托盘点击打开的文件路径：服务返回 WSL 路径（`/mnt/c/...`），托盘侧转换为 Windows 路径（见 `crates/anm-tray-win/src/wslpath.rs`）；非 `/mnt` 路径（跨机时服务端笔记在别处）转换后本机不存在 → 系统打开失败时**轻提示**，不弹系统错误框；**文本编辑不受影响**（内容经 IPC 由 core 读写）。
- 访问令牌：服务端 `[server] token` 配置后，托盘「设置服务地址…」对话框或环境变量 `ANM_SERVER_TOKEN` 需填写一致令牌；详见 readme.md §17 访问安全。

---

## 4. 开放问题

见 readme.md §15，实现中遇到先停下与作者确认，不要自行拍板：

1. 常识性事实的新鲜度/失效机制
2. 跨 session 的短期接续（recall 粒度）
3. 自由思维场景 exhibit 的相关性/意外性配比（需可调参数）
4. 是否需要更多场景类型
5. 分诊（triage）的触发方式（倾向：决策场景会前准备）
6. 命名空间标签的语义与检索（明确搁置）
7. 标签图的具体数据结构（是否权重、是否类型化边）
