# anm 功能设计文档

> 本文档描述 anm（anotemanager）的功能设计与架构决策。
> 状态：**架构讨论中，尚未实施**。凡标注「待定」处均未定稿。

---

## 0. 功能总览

> 阶段说明：**核心**＝本次实现范围；**待定**＝方向已认可、细节未定；**未来**＝未排期的规划；**已规划**＝既定规划。

| 类别 | 功能 | 一句话描述 | 阶段 |
|------|------|-----------|------|
| 标签系统 | 标签行识别 | 识别整行仅含一个或多个 `@xxx` 的行，每个 `@xxx` 视为一个标签 | 核心 |
| 标签系统 | 头部标签区同步 | 自动将文档标签维护至文档头部标签区，保持单一权威来源 | 核心 |
| 标签系统 | 标签反向索引 | 标签→文件的索引落盘存储，查询不现扫全目录 | 核心 |
| 标签系统 | 标签查询 | 按一个或多个标签筛选笔记 | 核心 |
| 标签系统 | 标签补全 | 输入时补全系统中已存在的标签 | 待定 |
| 标签系统 | 标签重命名 | 批量重命名某标签并同步头部标签区与索引 | 未来 |
| 标签系统 | 标签合并/拆分 | 将一个标签合并进另一个，或拆分出新标签 | 未来 |
| 标签系统 | 标签统计 | 统计每个标签覆盖的笔记数与使用频率 | 未来 |
| 标签系统 | 悬空标签检测 | 检测没有关联笔记的孤立标签 | 未来 |
| 标签系统 | 内部层级模型 | 文档内标签永远扁平，层级仅在 anm 内部数据模型（扩展点） | 核心 |
| 查询浏览 | 目录浏览 | 按笔记系统目录结构浏览笔记 | 核心 |
| 查询浏览 | 标题查询 | 按文件名 / 标题检索笔记 | 待定 |
| 查询浏览 | 全文检索 | 对笔记正文进行关键词搜索 | 待定 |
| 查询浏览 | 组合筛选 | 标签、目录、标题多维度 AND / OR 组合筛选 | 待定 |
| 查询浏览 | 最近笔记 | 按修改时间列出最近更新的笔记 | 待定 |
| 查询浏览 | 结构化输出 | 支持 JSON 等机器可读输出，供脚本与 MCP 复用 | 核心 |
| CLI 交互 | 子命令体系 | 统一的 `anm <subcommand>` 命令结构 | 核心 |
| CLI 交互 | shell 补全 | 在 shell 中 tab 补全一级目录、子命令与标签 | 核心 |
| CLI 交互 | anw 快速写入 | 独立命令将参数追加写入默认 skatch.md（inbox 入口） | 核心 |
| CLI 交互 | TUI 界面 | 裸输 `anm` 进入笔记管理界面 | 核心 |
| CLI 交互 | 层级目录浏览 | TUI 默认按层级目录展示笔记系统 | 核心 |
| CLI 交互 | 编辑器打开 | 光标定位笔记后用配置的文本编辑器打开 | 核心 |
| CLI 交互 | 界面自定义 | TUI 布局、按键、行为可自定义 | 已规划 |
| CLI 交互 | AI 自定义 skill | 制作 skill 让 AI 协助完成 TUI 自定义 | 已规划 |
| CLI 交互 | 快捷跳转 | 输入路径 / 标签快速直达某条笔记 | 未来 |
| MCP | MCP Server | 通过 MCP 向 AI agent 暴露 anm 能力 | 核心 |
| MCP | agent 查笔记 | agent 按标签 / 目录 / 内容检索笔记 | 核心 |
| MCP | agent 写 inbox | agent 将内容写入 skatch.md 入闸 | 核心 |
| MCP | agent 标签维护 | agent 管理文档标签与反向索引 | 核心 |
| MCP | 记忆总线 | 作为 agent 跨会话的第一内存取指-加载通道 | 核心 |
| 数据索引 | 配置管理 | `~/.anm/` 配置：笔记系统注册、编辑器、标签区格式等 | 核心 |
| 数据索引 | 索引落盘 | 标签 / 元数据索引持久化，避免每次全量扫描 | 核心 |
| 数据索引 | 增量更新 | 检测外部文件变更并增量刷新索引 | 核心 |
| 守护进程 | 后台常驻 | 以守护进程 / 服务形式常驻后台，持续维护索引与视图 | 核心 |
| 守护进程 | 文件监听 | 监听笔记目录变化，实时感知外部修改 | 核心 |
| 守护进程 | 索引实时维护 | 文件变动时增量刷新索引，无需手动重建 | 核心 |
| 守护进程 | IPC 服务 | 通过 IPC 向 CLI / MCP / 托盘提供查询服务 | 核心 |
| 守护进程 | 自动拉起 | CLI / MCP 调用时自动确保守护进程在运行 | 核心 |
| 守护进程 | 非唯一入口 | 守护进程只是观察者与索引维护者，不拦截任何外部修改 | 核心 |
| Windows | 托盘程序 | Windows 系统托盘常驻图标，支持 Windows 下使用 anm | 核心 |
| Windows | 快捷键呼出 | 全局快捷键呼出交互窗口与 anm 交互 | 核心 |
| Windows | 交互窗口 | 快捷键呼出的快捷输入 / 搜索窗口，快速与 anm 交互 | 核心 |
| Windows | 薄壳原则 | Windows 侧仅为薄壳，主要功能全部运行于 Linux / WSL | 核心 |
| Windows | TCP 通信 | 托盘经 TCP 连接运行于 Linux / WSL 的守护进程 | 核心 |
| 数据索引 | 多系统支持 | 注册并管理多个笔记系统（现阶段默认一个） | 未来 |
| 生命周期 | 新建笔记 | 在指定目录创建新笔记 | 待定 |
| 生命周期 | 重命名 / 移动 | 移动或重命名笔记并同步索引（引用更新后置） | 待定 |
| 生命周期 | 删除 | 删除笔记并同步索引 | 待定 |
| 生命周期 | inbox 分流 | 把 skatch.md 条目分流至合适的板块（消费回路） | 待定 |
| 生命周期 | 归档 | 将笔记归档至归档目录 | 待定 |
| 生命周期 | 笔记模板 | 新建笔记时套用预置模板 | 未来 |
| 统计洞察 | 使用统计 | 记录笔记消费频率、访问路径、点击序列 | 未来 |
| 统计洞察 | 认知洞察 | 从使用数据反推注意力流向与认知模式 | 未来 |
| 统计洞察 | 操作日志 | 记录 anm 操作历史供回溯 | 未来 |
| 统计洞察 | 导出 | 导出笔记 / 索引为可移植格式 | 未来 |

---

## 1. 架构

```
anm_core  (lib)  — 核心库，实现全部基本功能
├── config   — ~/.anm/ 配置加载
├── tags     — 标签行解析、标签反向索引、头部标签区同步
├── query    — 笔记查询（标签 / 目录 / 标题）
├── tree     — 一级目录枚举（供 shell 补全与 TUI）
├── inbox    — skatch.md 追加逻辑（anw 与 CLI 复用）
└── watch    — 文件监听、增量索引更新

anm_daemon (bin) — 后台守护进程：常驻、监听笔记变动、维护索引、提供 IPC（薄壳）
anm_cli    (bin) — CLI + shell 补全脚本 + TUI 界面（薄壳）
anm_mcp    (bin) — MCP server，暴露 anm_core 能力（薄壳）
anw        (bin) — 把后续参数写入默认 skatch.md（薄壳）
anm_tray   (bin) — Windows 托盘（薄壳）：常驻托盘 + 全局快捷键呼出交互窗口，经 TCP 连接 WSL 内的守护进程
```

原则：**所有逻辑在 anm_core**，anm_daemon / anm_cli / anm_mcp / anw / anm_tray 只做接口适配。
守护进程**不是唯一入口**：用户可随时用任何形式修改笔记目录，守护进程只负责观察与索引维护。
Windows 侧（anm_tray）**亦为薄壳**：主要功能全部运行于 Linux / WSL，托盘经 **TCP** 连接守护进程。
